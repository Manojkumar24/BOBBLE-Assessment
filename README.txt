First Question:

Intially Reading the CSV file and adding the words and frequency into the Trie data structure.

Next taking string as an input to search for it.

Printing the frequency if the word is present.

Printing No if the word is not present. 

Comlpie code using command: 
	g++ find_the_word.cpp
	
run using command : 
	a.exe <path to CSV file>
	

Second Question:

Intially Reading the CSV file and adding the words and index ot it into the Trie data structure.

Next taking index as an input to search for it.

Printing the word if the word with at that index is present.


Comlpie code using command: 
	g++ reverse_search.cpp
	
run using command : 
	a.exe <path to CSV file>
